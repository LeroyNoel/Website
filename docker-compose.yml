services:
  nginx:
    image: nginx:alpine
    ports:
      - "30800:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
      - ./style.css:/usr/share/nginx/html/style.css:ro
      - ./Eccera header.png:/usr/share/nginx/html/Eccera header.png:ro
    restart: unless-stopped
